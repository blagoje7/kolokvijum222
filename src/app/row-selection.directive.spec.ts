import { RowSelectionDirective } from './row-selection.directive';

describe('RowSelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new RowSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
